# Dependencies
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
# NOTE: Lockfiles (package-lock.json, yarn.lock) should be COMMITTED
# They ensure reproducible builds across environments

# Environment variables
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
.env.*.users  # Simulated user credentials - never commit!

# Build outputs
dist/
build/
.next/
out/
*.js.map

# Docker volumes
postgres_data/
redis_data/

# IDE
.vscode/
.idea/
*.swp
*.swo
*~
.project
.classpath
.settings/

# OS
.DS_Store
Thumbs.db
desktop.ini

# Logs
*.log
logs/
build-log.txt
build-output.log

# Testing
coverage/
.nyc_output/
*.coverage
.jest-cache/
test-results/
playwright-report/

# TypeScript
*.tsbuildinfo

# Turborepo
.turbo/

# Temporary files
*.tmp
*.temp
nul
check-docker.sh

# Claude Code transcripts (commit these for documentation)
# Uncomment to exclude from git:
# docs/claude-sessions/

# macOS
.AppleDouble
.LSOverride

# Linux
*~
.fuse_hidden*
.directory
.Trash-*

# Windows
Desktop.ini
$RECYCLE.BIN/
*.cab
*.msi
*.msix
*.msm
*.msp
*.lnk

# Expo / React Native
mobile/.expo/
mobile/.expo-shared/
mobile/expo-env.d.ts
*.jks
*.p8
*.p12
*.key
*.mobileprovision
*.orig.*
web-build/

# Metro
.metro-health-check*
.claude/settings.local.json

# Claude Code transcripts (managed in separate repo: karmyq-claude-transcripts)
.claude-transcripts/
docs/claude-sessions/

# GitHub Actions
actions-runner/
.runner/
_work/
HANDOFF.md
packages/shared/dist
.worktrees/
.claude/worktrees/
.superpowers/
# Installed hook artifacts, not sources. husky is no longer a dependency, but it left
# core.hooksPath pointing here on some machines. The hook SOURCES live in
# scripts/git-hooks/; `npm run hooks:install` copies them into whichever directory git
# actually reads. Nothing in .husky/ should be edited or committed.
.husky/

# Auto-generated by npm run analyze:services (pre-commit hook)
services/dependency-graph.md
services/impact-analysis.md
services/version-drift.md

# Playwright MCP scratch artifacts (page/console dumps)
.playwright-mcp/
